server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server #eureka服务的主机名
  client:
    fetch-registry: false #不从Eureka服务上拉去服务的注册信息
    register-with-eureka: false #不把自己注册到服务上
    service-url:
     defaultZone: http://localhost:8761/eureka/


