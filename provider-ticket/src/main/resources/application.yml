server:
  port: 8002

spring:
  application:
    name: provider-ticket

eureka:
  instance:
    prefer-ip-address: true #根据服务ip地址注册自己
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka/


